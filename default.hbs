<!DOCTYPE html>
<html lang="{{@site.locale}}">

<head>

    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{!-- <link rel="stylesheet" type="text/css" href="{{asset 'built/screen.css'}}" /> --}}
    <link rel="stylesheet" type="text/css" href="{{asset 'built/main.css'}}" />
    <link rel="stylesheet" href="{{asset 'built/highlightjs/styles/stackoverflow-dark.min.css'}}">
    <title>{{meta_title}}</title>

    {{ghost_head}}
    {{!-- Outputs important meta data and settings, should always be in head --}}

</head>

<body class="{{body_class}}">

    {{> "molecules/header"}}

    <main role="main" class="">
        {{{body}}}
        {{!-- All content gets inserted here, index.hbs, post.hbs, etc --}}
    </main>

    {{> "molecules/footer"}}

    {{ghost_foot}}
    {{!-- Outputs important scripts - should always be included before closing body tag --}}
        <script src="{{asset 'built/jquery-3.4.1.min.js'}}"></script>

    {{#if pagination.pages}}
    <script>
        var maxPages = parseInt('{{pagination.pages}}');
    </script>
    <script src="{{asset 'built/infinitescroll.js'}}"></script>
    {{/if}}

    <script>
        $(document).ready(function () {
            // Mobile Menu Trigger
            $('#burger').click(function () {
                $('body').toggleClass('gh-head-open');
            });
        });
    </script>
    <script src="{{asset 'built/highlightjs/highlight.min.js'}}"></script>
    <script>hljs.highlightAll();</script>
</body>

</html>