{{!< default}}

<div class="">
    <div class="l-grid">

        <div class="grid-cols-3">
        {{#foreach posts}}

            {{> "card"}} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

    </div>
</div>
